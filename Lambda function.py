# Lambda function in Python is a small, simple, one-line function that you can write without using the word

area = lambda l,h: 0.5 * l * h      # This the same as 'def area(l,h): return 0.5 * l * h'
print(area(20,4))